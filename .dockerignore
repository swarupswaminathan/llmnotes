# Secrets — never copy into the image
.env
.env.*
!.env.example

# Local / VCS
.git
.gitignore
.venv
venv
__pycache__
**/__pycache__
*.py[cod]
*.egg-info
.pytest_cache
.mypy_cache
.ruff_cache
.cursor
.vscode

# Data, results, notebooks artifacts (mount at runtime if needed)
data/
results/
*.xlsx

# Docs / local scratch (optional; keep image lean)
*.md
!README.md
*.ipynb
